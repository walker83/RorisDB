[package]
name = "mongodb-protocol"
version = "1.1.0"
edition = "2024"
license = "Apache-2.0"
description = "MongoDB wire protocol implementation for HarnessDB - ApsaraDB MongoDB compatible"

[dependencies]
common = { workspace = true }
tokio = { workspace = true }
bytes = "1"
tracing = { workspace = true }
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
bson = "2"

# For data storage
dashmap = "5"
parking_lot = "0.12"
regex = "1"
